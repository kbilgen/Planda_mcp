/**
 * Planda MCP Server — Shared Constants
 */

export const API_BASE_URL = "https://app.planda.org/api/v1";

/** Maximum characters returned in a single response before truncation */
export const CHARACTER_LIMIT = 150000;

/** Default timeout for API requests (ms) */
export const REQUEST_TIMEOUT_MS = 30_000;
